# Contenido personal / generado por la app (vídeos, fotos, salidas de conversión)
private/
DCIM/
conversion/
estabilizado/
recompresion/
montaje/
.miniaturas/
.vidstab_cache/
.proxies/
.ubicaciones.json
_originales_sin_gps/
*.MTS
*.M2TS
*.mp4
*.mov
*.m4v
*.jpg
*.jpeg
*.png
*.heic
*.heif
*.tif
*.tiff
*.CTG

# Python
__pycache__/
*.pyc
.venv/
venv/

# macOS
.DS_Store

# Docker
.env

# Configuración local de Claude Code
.claude/

# Datos de WhatsApp: bases descifradas, copias cifradas y registros. Nunca al repo —
# msgstore.db son TODAS las conversaciones en claro.
whatsapp-datos/
*.crypt15
*.crypt14
msgstore*.db
wa.db

# Agendas exportadas para poner nombre a los números: nombres y teléfonos de gente real,
# y basta un `git add .` para publicarlos. Se leen desde donde estén, no hace falta que
# vivan en el proyecto.
*.vcf
*.vcard
contacts.csv
contactos*.csv

# Listas de avatares extraídas de WhatsApp Web: nombres reales y URLs firmadas.
avatares-whatsapp*.json
